# מדריך חיבור לגוגל שיטס

זמן ביצוע: **~10 דקות**. החשבון שנמצא בשימוש: `2byproduction@gmail.com`.

---

## שלב 1 — יצירת הגיליון (1 דקה)

1. כנסי ל־[sheets.google.com](https://sheets.google.com) עם החשבון `2byproduction@gmail.com`
2. צרי גיליון חדש → שם: **"BY Warehouse"**
3. העתיקי את ה־URL של הגיליון (לגיבוי)

---

## שלב 2 — הדבקת הקוד (2 דקות)

1. בתוך הגיליון: **Extensions → Apps Script**
2. תיפתח לשונית עם קובץ `Code.gs` — מחקי הכל
3. פתחי את הקובץ `apps-script.gs` (נמצא בפרויקט) והעתיקי את **כל התוכן**
4. הדביקי ב־`Code.gs`
5. לחצי **Save** (אייקון דיסקט) — תתבקשי לתת שם לפרויקט, למשל "BY Warehouse Backend"

---

## שלב 3 — הרצת setupSheets (1 דקה)

1. בקוד, בחרי מהתפריט העליון את הפונקציה **`setupSheets`**
2. לחצי **Run** (כפתור הפעלה)
3. תופיע בקשת הרשאות → **Review permissions** → בחרי בחשבון `2byproduction@gmail.com` → **Advanced → Go to BY Warehouse (unsafe)** → **Allow**
   - ("unsafe" מופיע כי זה סקריפט שלך, לא של צד שלישי. זה רגיל.)
4. אחרי שהפונקציה רצה תקבלי הודעה: "✓ כל הלשוניות נוצרו בהצלחה"
5. חזרי לגיליון — תראי 5 לשוניות חדשות: `products`, `events`, `contacts`, `reservations`, `history`

---

## שלב 4 — פריסה כ־Web App (3 דקות)

1. ב־Apps Script: בפינה הימנית־עליונה **Deploy → New deployment**
2. לחצי על אייקון ההילה (⚙️) → בחרי **Web app**
3. הגדרות:
   - **Description:** `BY Warehouse API v1`
   - **Execute as:** `Me (2byproduction@gmail.com)`
   - **Who has access:** `Anyone`
     (**חשוב:** "Anyone" — כי רק עם URL הסודי ניגשים, אבל מי שיש לו URL ייגש בלי Auth)
4. לחצי **Deploy** → עוד פעם **Authorize access**
5. **העתיקי את ה־Web app URL** — נראה משהו כמו:
   `https://script.google.com/macros/s/AKfycby.../exec`

---

## שלב 5 — חיבור האפליקציה (1 דקה)

1. באפליקציה: **הגדרות** (סמל גלגל השיניים בסרגל הצד)
2. הדביקי את ה־URL בשדה "Google Sheets API URL"
3. לחצי **בדיקת חיבור** → אמור להופיע ✓ ירוק
4. לחצי **סנכרון ראשוני** → המוצרים והאירועים הקיימים באפליקציה יועלו לגיליון

מרגע זה:
- כל שינוי באפליקציה → נשמר אוטומטית בגיליון
- כל שינוי בגיליון → יעודכן באפליקציה בריענון
- תמונות נשמרות לתיקייה **"BY Warehouse Images"** ב־Drive (נוצרת אוטומטית)

---

## תחזוקה

**הוספת מוצר ידנית בגיליון:** פשוט הוסיפי שורה חדשה ב־`products`. המזהה (id) חייב להיות ייחודי, למשל `CA-012`.

**עריכה באפליקציה נכשלת:** בדקי שה־URL נכון, ושהפריסה עדיין פעילה (Deploy → Manage deployments).

**רוצה לראות את כל הבקשות:** ב־Apps Script, **Executions** בסרגל הצד — כל קריאה מופיעה עם לוג.

**גיבוי:** גיליון Google מגובה אוטומטית. בנוסף כדאי פעם בחודש: File → Download → xlsx.
